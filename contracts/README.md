# Contracts

This directory stores exported JSON schemas for the invention-mode entities.

Canonical model definitions live in `src/dossier/contracts`.

Regenerate the exported schemas with:

```powershell
python scripts\export_contract_schemas.py
```

